import React, {PureComponent} from 'react';

export default class Legend extends PureComponent {

    render() {
        let name = 'legend';

        return (
            <div>
                {`hello ${name}`}
            </div>
        );
    }
}
